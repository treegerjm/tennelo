% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rankings.R
\name{compare_rankings}
\alias{compare_rankings}
\title{Compare Elo with the official ATP ranking}
\usage{
compare_rankings(
  fit,
  rankings,
  as_of = NULL,
  active_within = 365,
  min_matches = 20,
  n = 20
)
}
\arguments{
\item{fit}{An object from \code{\link[=fit_elo]{fit_elo()}}.}

\item{rankings}{A data frame from \code{\link[=prepare_rankings]{prepare_rankings()}}.}

\item{as_of}{The Elo cut-off date. \code{NULL} uses the fit's last date.}

\item{active_within, min_matches}{Passed to \code{\link[=elo_leaderboard]{elo_leaderboard()}} to decide who
is eligible for comparison.}

\item{n}{Size of the top-n overlap statistic.}
}
\value{
An object of class \code{tennelo_comparison}.
}
\description{
Takes the most recent ATP ranking that is not \emph{after} \code{as_of}, joins it to
the Elo ratings on player id, and reports where the two disagree.
}
\details{
The comparison is only called same-day when the two dates actually match;
otherwise the ranking date used is reported alongside the gap in days.
Nothing about the expected result is hard-wired: no particular player is
assumed to lead, and no top-n overlap is treated as proof of correctness.
}
