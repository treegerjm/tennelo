% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elo_core.R
\name{elo_match_k}
\alias{elo_match_k}
\title{Combine two K-factors into a single match K-factor}
\usage{
elo_match_k(k_a, k_b)
}
\arguments{
\item{k_a, k_b}{Strictly positive, finite K-factors.}
}
\value{
The geometric mean \code{sqrt(k_a * k_b)}.
}
\description{
Used by the \code{"symmetric"} pairing rule, which gives both players the same
K-factor so that the rating pool keeps its total.
}
\details{
Equal inputs return that value bit-for-bit. This is a deliberate
short-circuit: \code{sqrt(k * k)} is not exactly \code{k} for every representable \code{k},
and \code{elo_match_k(k, k) == k} is a property worth having exactly rather than
approximately.
}
\examples{
elo_match_k(131.33, 38.86)
elo_match_k(50, 50)   # exactly 50
}
