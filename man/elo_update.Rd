% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elo_core.R
\name{elo_update}
\alias{elo_update}
\title{Update two ratings after a match}
\usage{
elo_update(rating_a, rating_b, score_a, k_a, k_b = k_a)
}
\arguments{
\item{rating_a, rating_b}{Finite numeric ratings before the match.}

\item{score_a}{\code{1} if player A won, \code{0} if player A lost. Tennis has no
draws, so \code{0.5} is an error.}

\item{k_a}{K-factor applied to player A.}

\item{k_b}{K-factor applied to player B. Defaults to \code{k_a}.}
}
\value{
A list with the new ratings \code{a} and \code{b} and the amounts transferred,
\code{delta_a} and \code{delta_b}.
}
\description{
Update two ratings after a match
}
\details{
The default \code{k_b = k_a} is what selects the pairing rule, so that the core
needs no mode switch:
\itemize{
\item \strong{\code{"individual"}} — pass each player's own K-factor. This matches the
convention of published tennis Elo lists and lets an established rating
resist a single result. The rating pool is then not conserved; it drifts by
exactly \code{(k_a - k_b) * delta}, which is a testable identity rather than an
unbounded error.
\item \strong{\code{"symmetric"}} — pass \code{elo_match_k(k_a, k_b)} for both, or rely on the
default. The transfers are then equal and opposite by construction, and
\code{delta_b == -delta_a} holds bit-for-bit.
}
}
\examples{
# Two debutants, so both K-factors agree and the pool is conserved.
elo_update(1500, 1500, score_a = 1, k_a = elo_k(0))

# A debutant beats a veteran, each with their own K-factor.
elo_update(1500, 2000, score_a = 1, k_a = elo_k(0), k_b = elo_k(100))
}
