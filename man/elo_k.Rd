% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elo_core.R
\name{elo_k}
\alias{elo_k}
\title{Experience-dependent K-factor}
\usage{
elo_k(matches_played, k_start = 250, offset = 5, shape = 0.4)
}
\arguments{
\item{matches_played}{Whole numbers \verb{>= 0}: matches played \emph{before} the
match being rated.}

\item{k_start, offset, shape}{Model parameters. The defaults
(\code{250}, \code{5}, \code{0.4}) follow the convention common in tennis Elo work, but
they are parameters of this model, not universal constants.}
}
\value{
A numeric vector of strictly positive K-factors, decreasing in
\code{matches_played}.
}
\description{
The K-factor sets how far a single result moves a rating. It decays with the
number of matches a player has already had, so that a newcomer's rating
finds its level quickly while an established rating stays stable.
}
\details{
\code{K(m) = k_start / (m + offset)^shape}. With the defaults, a debutant gets
\code{K(0) = 131.33} and a player with 100 matches gets \code{K(100) = 38.86}.
}
\examples{
elo_k(0)     # 131.3264
elo_k(100)   # 38.85655
}
